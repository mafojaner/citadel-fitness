/**
 * Served by GitHub Pages from docs/index.html on this repo's main branch.
 * Contact email and developer name are filled in with real values; the
 * page's own "Before you publish this" callout is a standing reminder that
 * a qualified lawyer should still review it before payments or new regions
 * are added, not a placeholder that still needs data. Confirm Pages is
 * enabled in the repo's Settings and this URL actually resolves before
 * relying on it for an App Store submission.
 */
export const PRIVACY_POLICY_URL = 'https://mafojaner.github.io/citadel-fitness/';
